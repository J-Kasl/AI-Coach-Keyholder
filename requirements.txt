# Phase 0 — communication layer, DB, config
discord.py>=2.7.1

# Phase 1+ (the Ollama client currently just uses plain requests/httpx over its REST API)
requests>=2.32.0

# Note: SQLite is part of the Python stdlib, no dependency needed.
# Note: .env is parsed by our own minimal loader in core/config.py --
#       python-dotenv is deliberately not used (see the comment in that file).
